organization: testing_redocly
apis:
  order_direct@v2:
    root: ./openapi/order-direct.yaml
    lint:
      rules:
        no-ambiguous-paths: error  

lint:
  extends:
    - recommended

features.openapi:
  schemaExpansionLevel: 2
  generateCodeSamples:
    languages:
      - lang: curl
      - lang: Python
  theme:
    colors:
      primary:
        main: "red"   